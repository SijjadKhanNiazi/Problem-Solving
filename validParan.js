const paren = (arr) => {
  //   const stackie = [];
  //   for (let i = 0; i < arr.length; i++) {
  //     if (arr[i] === "(" || arr[i] === "{" || arr[i] === "[") {
  //       stackie.push(arr[i]);
  //     } else {
  //       let poped = stackie.pop();
  //       if (poped === "(" && arr[i] !== ")") return false;
  //       if (poped === "{" && arr[i] !== "}") return false;
  //       if (poped === "[" && arr[i] !== "]") return false;
  //     }
  //   }
  //   return stackie.length === 0;
  // };
};

console.log(paren("([}])"));
