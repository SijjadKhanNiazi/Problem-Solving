const reverse = (str) => {
  let arr = str.split(" ");
  return arr.reverse();
};

console.log(reverse("sijjad khan niazi"));

//answer shoulde= khan sijjad
/* make then array of words not characters then sort((a,b)=>b-a)
 */
